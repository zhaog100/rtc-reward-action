name: Test RTC Reward Action

on:
  pull_request:
    types: [closed]

jobs:
  test-dry-run:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: ./  # Use local action for testing
        with:
          node-url: https://50.28.86.131
          amount: 5
          wallet-from: test-fund
          admin-key: test-key
          dry-run: true
